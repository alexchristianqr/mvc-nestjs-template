datasource db {
  provider = "sqlite"
  url = env("DATABASE_URL")
}

// Generator
generator client {
  provider = "prisma-client-js"
}

// Data model example
model Example {
  id Int @id @default(autoincrement())
  name String?
}